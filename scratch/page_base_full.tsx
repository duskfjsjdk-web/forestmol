"\"use client\";\n\nimport React, { useEffect } from 'react';\nimport Link from 'next/link';\nimport Navbar from '@/components/landing/Navbar';\nimport Hero from '@/components/landing/Hero';\nimport QuickLinkBar from '@/components/landing/QuickLinkBar';\nimport { ModalProvider, useModal } from '@/components/landing/ModalSystem';\n\nfunction MainLandingContent() {\n  const { openModal } = useModal();\n\n  // 스크롤 Reveal 애니메이션 감지\n  useEffect(() => {\n    const revealEls = document.querySelectorAll('.rv');\n    const observer = new IntersectionObserver((entries) => {\n      entries.forEach(entry => {\n        if (entry.isIntersecting) {\n          entry.target.classList.add('on');\n        }\n      });\n    }, { threshold: 0.1 });\n\n    revealEls.forEach(el => observer.observe(el));\n    return () => observer.disconnect();\n  }, []);\n\n  return (\n    <main className=\"min-h-screen bg-[var(--white)] font-dmsans text-[var(--ink)]\">\n      \n      {/* 네비게이션 및 히어로 퀵링크 통합 영역 */}\n      <div className=\"relative h-screen min-h-[700px]\">\n        <Navbar />\n        <Hero />\n        <QuickLinkBar />\n      </div>\n\n      {/* ────────────────────────────────────────────────────────\n          1. FEATURES SECTION\n          ──────────────────────────────────────────────────────── */}\n      <section id=\"features\" className=\"py-[108px] px-6 md:px-14 bg-[var(--white)]\">\n        <div className=\"max-w-[1200px] mx-auto\">\n          \n          {/* Eyebrow */}\n          <div className=\"flex items-center gap-3 text-[10px] tracking-[0.2em] uppercase text-[var(--muted)] font-dmmono mb-5 rv\">\n            <span className=\"w-5 h-[1px] bg-[var(--rule)]\"></span>\n            <span>핵심 기능</span>\n          </div>\n\n          {/* H2 Title */}
<truncated 16762 bytes>